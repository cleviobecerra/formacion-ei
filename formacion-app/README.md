# Formación EI (app)

Next.js 16 + Supabase + shadcn/ui.

```bash
copy .env.example .env.local
npm install
npm run dev
```

Aplica la migración en `supabase/migrations` sobre tu proyecto de Supabase y asigna el primer `admin` desde SQL.
